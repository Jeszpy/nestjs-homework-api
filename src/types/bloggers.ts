export type BloggerType = {
    id: string,
    name: string,
    youtubeUrl: string
}

export type BloggerWithDateType = {
    id: string,
    name: string,
    youtubeUrl: string,
    deletedAt: Date
}